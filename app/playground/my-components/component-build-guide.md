# How to Build a Component

1. Build your component only in the `my-components` folder (`src/app/playground/my-components`).
2. Name your component as you like.
3. Add (register) your component in `index.tsx`.
4. Done! Start working on your component.

open localhost/playground to test your component
